# Copy to .env.runner and never commit the copy.
GITHUB_REPOSITORY=owner/repository
GITHUB_TOKEN=replace-with-a-fine-grained-token
# Optional HMAC secret configured on the GitHub repository webhook.
WEBHOOK_SECRET=

